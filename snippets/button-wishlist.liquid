{% comment %}
  Shopify Wishlist
  Usage:
    - Markup: {%- render 'button-wishlist', product: product -%}
    - Place this snippet inside your existing product card snippet

  Parameters:
    - product: <Shopify product> (required)
{% endcomment %}
{%- if product.handle != blank -%}
  <button id="wishlist-custom" type="button" aria-label="Add to wishlist" class button-wishlist data-product-handle="{{ product.handle }}">
    <style scoped>
      .icon {
        fill: transparent;
        stroke: #000000;
        transition: fill 0.3s ease;
      }

      .active .icon {
        fill: yellow;
        stroke: #000000;
      }
      #wishlist-custom {
        z-index: 99999;
      }
    </style>
    {%- render 'icon-heart' -%}
  </button>
{%- endif -%}